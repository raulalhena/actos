export interface EventFormProps  {
    name: string,
    description: string;
    tags?: string[],
    category?: string, 
    adress?: string,
    date: string,
    startTime: string,
    endTime: string,
    timeZone?: string,
    showStartTime?: boolean,
    showEndTime?: boolean,
    confirmed?: boolean,
    type: string,
    mode: string,
    image?: string,
    video?: string,
    qr?: string[],
    attendees: string[],
    submitted: string[],
    capacity: number,
    price?: number,
    payment?: string,
    contact?: string,
    language: string[],
    web: string,
    visibility: boolean,
    status: boolean
}